# Aula 05: Formas Normais (FND/FNC) e Otimização Booleana

## 1. Fundamentos Matemáticos: Formas Canônicas e Minimização

Na álgebra proposicional, qualquer função lógica $f(x_1, x_2, \dots, x_n)$ pode ser expressa em duas formas canônicas padrão:

1. **Forma Normal Disjuntiva (FND / Soma de Produtos - SOP):**
   - Disjunção ($\lor$) de mintermos (termos conjuntivos contendo todas as variáveis ou suas negações).
   - $f = \bigvee_{m \in Mintermos} m$.
   - Representa os estados onde a saída do processo (ex: permissivo de decolagem) é **Verdadeira ($1$)**.

2. **Forma Normal Conjuntiva (FNC / Produto de Somas - POS):**
   - Conjunção ($\land$) de maxtermos (termos disjuntivos).
   - $f = \bigwedge_{M \in Maxtermos} M$.
   - Representa o isolamento e bloqueio de todas as combinações de falha **Falsas ($0$)** (ex: condições que forçam o pouso de emergência).

3. **Teoremas de Minimização (Quine-McCluskey e Mapas de Karnaugh):**
   - Otimização do número de literais e portas lógicas utilizando a lei do consenso e adjacência lógica:
     $$(A \land B) \lor (A \land \neg B) \equiv A \land (B \lor \neg B) \equiv A$$

---

## 2. Aplicação em Engenharia: Otimização de Varredura no Controlador de Voo e SCADA

Em sistemas autônomos críticos como o Drone Agrícola, o motor de intertravamento da controladora de voo e da estação base executa loops de avaliação de telemetria em frequências extremamente altas (frequentemente superiores a 400 Hz). 

Expressões booleanas longas e não otimizadas aumentam o consumo de processamento da CPU embarcada, reduzem a vida útil da bateria do drone e geram atrasos (*latency*) na tomada de decisão em situações de risco operacional (como rajadas de vento ou perda de sinal). A minimização algébrica garante um código executável enxuto e de resposta quase instantânea.

```mermaid
graph LR
    TruthTable["Tabela-Verdade de Telemetria (2^n estados)"] --> Minterms["Extração de Mintermos (FND Canônica)"]
    Minterms --> Algoritmo["Algoritmo de Minimização Booleana (Quine-McCluskey)"]
    Algoritmo --> EquacaoMin["Equação Booleana Mínima (FND Otimizada)"]
    EquacaoMin --> PLC["Execução em Tempo Real na Controladora / SCADA"]
